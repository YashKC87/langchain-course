# ============================================================
#  HEALIX — Python Dependencies
#  Install all of these with: pip install -r requirements.txt
# ============================================================

# Web framework (the server that handles API requests)
fastapi>=0.111.0
uvicorn[standard]>=0.30.1

# AI / LLM framework
langchain>=0.2.6,<1.0.0
langchain-openai>=0.1.13,<1.0.0
langchain-community>=0.2.6,<1.0.0

# Vector database (stores document embeddings for RAG)
faiss-cpu>=1.8.0

# OpenAI API client
openai>=1.35.3

# Environment variable management (reads .env file)
python-dotenv>=1.0.1

# Data validation
pydantic>=2.7.4

# HTTP requests (for calling external APIs)
requests>=2.32.3

# Async support
aiohttp>=3.9.5

# === Azure Identity & Authentication ===
azure-identity>=1.15.0
msal>=1.26.0

# === Microsoft Graph SDK ===
msgraph-sdk>=1.0.0

# === Azure Monitor / Log Analytics ===
azure-monitor-query>=1.3.0

# === Async SQLite ===
aiosqlite>=0.19.0

# === Background task scheduling ===
apscheduler>=3.10.4

# === Email (SMTP async) ===
aiosmtplib>=3.0.0

# === Async HTTP client ===
httpx>=0.26.0

# === AWS SDK (for EC2 / CloudWatch / SSM agentless polling) ===
boto3>=1.34.0
